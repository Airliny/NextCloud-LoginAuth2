# Changelog

## 1.0.0 (2026-02-01)

首个版本。

* 标准 OAuth 2.0 授权码模式接入 Airliny 统一认证中心，强制 PKCE (S256) 与 state 校验
* 仅匹配已有 Nextcloud 账号登录（无 JIT 自动建号）
* 匹配策略可配置：邮箱优先 / 用户名优先 / 仅邮箱 / 仅用户名
* SSO 身份绑定表：sub ↔ uid 锁定绑定，防止账号顶替；管理面板可查看与解除绑定
* Client Secret 使用实例密钥加密存储
* 登录页注入 SSO 按钮；可选自动跳转（带 `?noredir=1` 逃生通道）
* 可选隐藏本地密码表单（仅前端展示层）、同步显示名
* 兼容 Nextcloud 31 – 34，提供简体中文 / 英文文案
